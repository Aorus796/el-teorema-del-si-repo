{
  "name": "el-teorema-del-si",
  "version": "0.5.0",
  "private": true,
  "description": "Aventura narrativa de puzles matematicos creada como regalo de boda.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node tools/dev-server.mjs",
    "build": "node tools/build.mjs",
    "preview": "node tools/dev-server.mjs builds/browser 4173",
    "test": "node --test",
    "check": "npm run test && npm run build",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
