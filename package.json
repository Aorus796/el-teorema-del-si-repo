{
  "name": "el-teorema-del-si",
  "version": "1.1.0",
  "private": true,
  "description": "Aventura narrativa de puzles matematicos creada como regalo de boda.",
  "type": "module",
  "main": "electron/main.js",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node tools/dev-server.mjs",
    "build": "node tools/build.mjs",
    "preview": "node tools/dev-server.mjs builds/browser 4173",
    "test": "node --test",
    "check": "npm run test && npm run build",
    "test:e2e": "playwright test",
    "verify": "npm run check && npm run test:e2e",
    "desktop:dev": "npm run build && electron .",
    "desktop:package:win": "npm run build && electron-builder --win portable --x64 --publish never"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "electron": "43.3.0",
    "electron-builder": "26.15.7"
  }
}
